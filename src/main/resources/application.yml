spring:
  datasource:
    url: jdbc:h2:file:./data/pmp;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password: 1qaz@WSX
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
  h2:
    console:
      enabled: true
      path: /h2-console

  thymeleaf:
    cache: false

server:
  port: 9082
  ssl:
    enabled: true
    key-store: file:/opt/wzw/cert/sql2mongo.com.jks
    key-store-password: 1m7f26zc68wel95
    key-store-type: JKS

logging:
  level:
    com.pmp: DEBUG
